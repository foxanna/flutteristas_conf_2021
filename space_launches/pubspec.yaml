name: space_launches
description: Space missions launches calendar
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  freezed_annotation: ^0.14.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner:
  freezed: ^0.14.1+1
  json_serializable: ^4.1.0

flutter:
  uses-material-design: true
